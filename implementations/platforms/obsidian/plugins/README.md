# Plugins

This directory contains the plugins used by the Obsidian platform implementation.

Each plugin is documented independently, including:

- Purpose
- Installation
- Configuration
- Supported version
- Integration with Ohtli

Plugin configuration files are stored in the Vault (`vault/.obsidian/`) while this directory contains the implementation documentation and related resources.
